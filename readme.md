# Steam Inspect-Link Scraper

```bash
git clone …/steam-inspect
cd steam-inspect
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# Run it (default: AWP | Printstream Field-Tested)
python scraper.py
